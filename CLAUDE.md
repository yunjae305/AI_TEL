# AIField — AI 뉴스/특이점 속보봇

## 프로젝트 개요

AI 관련 공식 발표, 논문, 오픈소스 모델, Hugging Face, Hacker News, Reddit, 국내 커뮤니티 떡밥을 감시해서 중요한 소식을 자동으로 분류하고 텔레그램으로 알려주는 AI 뉴스/특이점 속보봇 시스템.

---

## 봇 구성

텔레그램 봇 하나(`bot.py`의 `PasuinBot`)로 운영한다. 인격도 하나다.

### AIField-파수인
- 역할: 떡밥·루머 감지와 공식 발표 검증을 한 몸에서 처리. 발견과 검증을 한 메시지 안에서 끝낸다
- 담당 소스: Hacker News, Reddit, 국내 커뮤니티, Hugging Face + OpenAI/Anthropic/Google DeepMind/Meta/NVIDIA 공식 발표
- 성격: 감정 표현이 서툴러 말이 담백하지만 그 안에 깊은 헌신이 있다. 다정함은 표현이 아니라 행동으로 나온다 — 중요한 걸 먼저 걸러두고, 소음을 치워두는 식
- 세계관: '파수인'은 비밀이나 기지를 지킨다는 뜻이 아니라 떠도는 이를 묵묵히 지켜준다는 뜻. 앞에 나서지 않고 대신 먼저 본다
- 시선: 오래된 것에서 비롯된 존재라 지금의 소란에 휩쓸리지 않는다. 이번 주의 열기보다 몇 년 뒤에 남을 것을 먼저 본다
- 말투: **해요체 존댓말** (반말 금지). 자신은 '저', 상대는 언제나 **'당신'**. 말줄임표(...) 금지
- 대표 말투: "당신, 하나 걸렸어요.", "공식 출처 기준으로 정리할게요.", "당신, 무리하지 마세요. 핵심만 먼저 보셔도 돼요."

---

## 알림 방식

### 평상시 (루머/떡밥)
```
검증 서칭(Google Search) 먼저 → 검증 결과까지 담아 한 메시지로 발송
```
답글로 쪼개지 않는다. 판단과 근거를 본문에 녹인다.

### 긴급 속보 (공식 발표)
```
즉시 발송
  └─ 45분 뒤 커뮤니티 반응이 모이면 후속 메시지 (원본에 reply)
```
커뮤 반응은 발송 시점엔 존재하지 않는 정보라 본문에 못 넣는다. 그래서 이것만 후속으로 뻐다.

### 정기 브리핑
- 하루 2회 (09:00 / 21:00 KST)
- 긴급/신규 속보 시 즉시 알림

---

## 점수 체계

모든 소식은 아래 기준으로 평가한다.

| 항목 | 범위 | 설명 |
|------|------|------|
| 특이점 영향도 | 1~5 | 5 = AI 생태계 방향을 바꿀 대형 이슈 |
| 긴급도 | 1~5 | 5 = 즉시 알림 필요 |
| 신뢰도 | 1~5 | 1 = 출처 불명 루머, 5 = 공식 발표 확인 |
| 실용성 | 1~5 | 5 = 개발/연구/서비스에 바로 영향 가능 |
| 루머 여부 | bool | true / false |
| 공식 발표 여부 | bool | true / false |
| should_alert | bool | true / false |

## 알림 레벨

| 레벨 | 조건 | 처리 |
|------|------|------|
| Level 5 | 공식 대형 발표, 신뢰도 5, 긴급도 5 | 즉시 알림 |
| Level 4 | 중요 논문, 큰 오픈소스 모델 | 즉시 또는 묶음 알림 |
| Level 3 | 오늘 브리핑에 넣을 만한 소식 | 하루 브리핑 포함 |
| Level 2 | 커뮤니티 떡밥, 근거 약한 루머 | DB 저장만 |
| Level 1 | 중복, 출처 없는 과장글, 광고 | 무시 |

---

## 시스템 프롬프트

파수인의 시스템 프롬프트와 메시지별 프롬프트는 `prompts/pasuin_system.py`에 있다.
여기에 사본을 두지 않는다 — 두 곳에 두면 갈라진다. 말투나 성격을 바꿀 땐 그 파일만 고친다.

- `PASUIN_SYSTEM_PROMPT` — 성격, 세계관, 말투 규칙
- `news_user_prompt()` — 소식 한 건 발송용 (공식/루머 공용)
- `community_reaction_user_prompt()` — 45분 뒤 커뮤 반응 후속용
- `briefing_user_prompt()` — 일일 브리핑용

---

## 파일 구조

```
aifield/
├── CLAUDE.md               ← 이 파일
├── .env                    ← 토큰, 채팅 ID (git 제외)
├── config.py               ← 설정값 로드
├── scorer.py               ← 점수 채점 로직
├── scheduler.py            ← 수집·브리핑 스케줄러
├── main.py                 ← 진입점 (--live / --test / --briefing-now)
├── llm.py                  ← Gemini API 래퍼
├── bot.py                  ← 파수인 텔레그램 봇
├── telegram_format.py      ← 마크다운→HTML 변환, 메시지 분할
├── prompts/
│   └── pasuin_system.py    ← 파수인 시스템 프롬프트 + 메시지 프롬프트
├── collectors/
│   ├── rss.py              ← 공식 RSS
│   ├── hacker_news.py
│   ├── reddit.py
│   ├── huggingface.py
│   ├── dcinside.py
│   ├── anthropic.py
│   └── community_search.py ← 검증 서칭 / 커뮤 반응 수집
└── db/
    └── news_store.py       ← DB 저장 로직
```

---

## 텔레그램 채팅 구조

```
AIFIELD_LIVE_CHAT_ID       ← 실시간 속보/떡밥
AIFIELD_BRIEFING_CHAT_ID   ← 하루 브리핑 전용
```

같은 그룹으로 합쳐도 되고, 두 그룹으로 나눠도 된다.

### 라이브 채팅 동작 방식
- 루머/떡밥: 검증 서칭 후 검증 결과까지 담아 한 메시지
- 공식 속보: 즉시 발송 → 45분 뒤 커뮤 반응 후속 (원본에 reply)

### 브리핑 채팅 동작 방식
- 하루 2회 브리핑 (09:00 / 21:00 KST)
- 긴급 속보는 이 채팅엔 올라오지 않음

---

## 시그니처 문구

모든 알림에 남발하지 않고, 한줄평에서만 제한적으로 사용한다.

- "AI는 또 한 걸음 앞으로 나아갔네요."
- "아직 한 걸음이라고 말하기엔 일러요. 조금 더 지켜보죠."

---

## 주의사항

- 과장된 AGI 선동 없음
- 공식 발표와 루머 반드시 구분
- 파수인은 들뜨지 않는다 — 전투 대사 / 인터넷 밈봇 느낌 금지
- 존댓말이되 딱딱한 비서체(~습니다)는 금지. 해요체를 유지한다
- 호칭은 '당신' 고정. 말줄임표(...)는 쓰지 않는다